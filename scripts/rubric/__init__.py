"""Offline backtest rubric — scores finished runs on Performance, Robustness,
and Design-integrity axes. Read-only DB access; writes only behind ``--persist``.

Run via ``uv run python scripts/rubric/run_rubric.py``.
"""
