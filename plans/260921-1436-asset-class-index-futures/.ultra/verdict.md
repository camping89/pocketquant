# Verdict — ak:plan --ultra --advice, asset-class index futures (2026-09-21)

Verifier: kongming. All five candidates read in full; every load-bearing claim below was checked against the repository on branch `develop` (669 tests collected; `uv run lint-imports` prints `Contracts: 8 kept, 0 broken.`; `uv run ruff check src tests scripts` passes; `uv run ruff check .` reports 301 pre-existing errors; `uv run pyright src` reports 0 errors; `uv run ruff check --select DTZ` finds 3 in `src/`, 28 in `tests/`, 3 in `scripts/`, 21 under `plans/`).

## Hard-constraint screen

All five candidates pass every hard constraint: seven phases plus a usable `plan.md`; the Failure Protocol block is present verbatim in all 35 phase files (byte-compared); no credential value appears (only field names and `...` placeholders); no non-goal is violated (no futures broker, no roll modelling, no Databento/IBKR adapter, no plan-tier branch, margin accounting kept); no candidate copies `../pocketquant-config` contents into the repo. No disqualification.

## Scoring (1-20 per criterion)

| Candidate | 1 Faithfulness | 2 Evidence | 3 Handover | 4 Sequencing | 5 Actionability | Total |
|---|---|---|---|---|---|---|
| A | 18 | 17 | 14 | 18 | 18 | **85** |
| B | 15 | 14 | 16 | 17 | 16 | 78 |
| D | 15 | 13 | 14 | 15 | 15 | 72 |
| C | 13 | 14 | 14 | 16 | 14 | 71 |
| E | 13 | 12 | 12 | 13 | 14 | 64 |

## Ranking

1. **A (85).** The only candidate that found the latent P0 that would have undone every other plan: `SyncService._persist_bars` (`sync_service.py:153`) calls `SymbolRepository.upsert(Symbol.create(symbol=symbol))`, and `upsert` does `{"$set": doc}` with the full document (`symbol_repository.py:15-32`, verified). Once `Symbol` carries `asset_class`/`calendar_id`/`contract_spec`, the first `sync_1m` after seeding silently resets `ES1!:CME_MINI` to `crypto_spot`/`CRYPTO_24_7`/linear, which re-fires all five day-one P0s and, once the resolver cache expires, re-routes ES to Binance. A's Phase 2 Task 10 replaces the call with a `$setOnInsert` `touch()` before any futures symbol exists; B, C, D and E never mention the site. A's grounding is otherwise the tightest: DTZ per-file counts match ruff exactly for `src/tests/scripts`, `compose.local.yml` has no `app` service (verified), both `COMPOSITE_SYMBOL_RE` and `COMPOSITE_SYMBOL_PATTERN` plus the three `PATTERN` callers are named correctly, `cascade_aggregator.py:81` `datetime.min` (DTZ901) is real, `Interval.periods_per_year_for` has one production caller at `backtest_report_app_service.py:368`, README line 101 really says "7 import-linter contracts", the scraper really imports as `tvDatafeed` from git and `git` really is in the Dockerfile builder stage (lines 9-11), and every web/docs line reference sampled (add-symbol-dialog 21/23/24/72, backtest-form 44/72, data-health-row 50/55/72, market-data.ts 20/26/88, system-architecture 173/479/670/773/777/796, collection-erd.mmd:20) resolves. A also silently corrects both upstream errors: the advice's G1 metric names a non-existent `GET /api/v1/market-data/bars/...?interval=` route (the real route is `/api/v1/market-data/ohlcv/{symbol}/{interval}`, `market_data_ohlcv.py:37`, prefix `/api/v1` from `config.py:44`), and both documents call `GetOHLCVQuery` a Pydantic DTO when it is a `@dataclass` (`ohlcv_service.py:13-14`); A uses the right route and a `__post_init__`. Its one systematic defect costs it criterion 3: every phase gate runs `uv run ruff check .`, which fails today with 301 errors outside `src/tests/scripts` (and would gain 21 DTZ hits under `plans/`), so a Sonnet executor would hit the Failure Protocol at Phase 1 Task 11 and at five later gates until the scope is corrected to `src tests scripts`.

2. **B (78).** Best handover mechanics: baselines are exact (`668 passed, 1 skipped`, `Contracts: 8 kept, 0 broken.`, DTZ 3/31), every one of ~30 sampled line references is exact (`scheduler.py:28,214`, `main.py:42,52`, `main_extensions.py:138,152-155`, `services.py:31`, `sync_jobs.py:228,253,302,398`, `bar_app_service.py:88`, `ohlcv_service.py:84-86`, `backtest_report_app_service.py:370-373`, `config.py:6,74`), the same-commit constraint is verified with `git show --stat HEAD --name-only` listing exactly four paths, and a ninth import-linter contract keeps `pandas_market_calendars` behind the port. It loses on grounding and faithfulness: `uv add "tvdatafeed"` cannot work (PyPI returns 404 for `tvdatafeed`; the package is git-only), both Phase 5 Verify URLs drop the `!` (`ES1%3ACME_MINI`), `_CASCADE_DEFAULT_INTERVALS` is really `_CASCADE_DEFAULT_TFS` (`tracked_symbols_backfill.py:31`), the 24/7 calendar is placed in `core/infra` against the advice's explicit domain placement, futures `DAY_1` is still cascaded rather than fetched natively, and the sync-clobber bug is missed.

3. **D (72).** Strongest on the audit's own error: it proves from `uv.lock` (lines 669 and 1283, markers `win32`/`emscripten` only) that the PyPI `tzdata` pin does not reach Linux, and adds OS `tzdata` to the runtime stage with a `docker run` verify; DTZ test count 28 is the only exact one; it has the complete `check_integrity`/`repair_integrity` caller inventory including `routes/integrity.py:33,45`; `pyright src` baseline is correct; `require_utc` matches the advice checklist. It is marked down for confidently wrong evidence in Phase 6 (`_calculate_pnl_per_unit` cited at 258-262, `reduce_quantity` realized at 189-190, `unrealized_pnl` at 268-270 — the real lines are 217, 149 and 224-227 in a 281-line file), a Verify against a non-existent route (`/api/v1/market-data/status`; the real one is `/sync-status`), a `grep -c "CME_MINI"` Verify that expects `2` on single-line JSON, a `GET /api/v1/system/jobs` list route that does not exist (`system_jobs.py` has only `/{job_id}/runs` and `/{job_id}/stats`), an unpinned "pick a commit" instruction for the scraper, and multiplying the `_can_afford` notional by the multiplier, which rejects a 2-contract ES entry against the default 10,000 balance and so blocks G2 unless the balance is raised.

4. **C (71).** Also catches the tzdata lock-marker error (via an explicit PyPI `tzdata` pin), uses `[UNVERIFIED]` honestly for the git dependency, gets every existing test count right (4/12/5/7/9/18/10), encodes `!` as `%21` correctly, and proves G4 with `inspect.getsource`. It deviates from R1 by keeping `asset_type` alongside `asset_class` (the requirement says "replaces"), overloads `commission_bps` as dollars-per-contract, multiplies `_can_afford` notional (same G2 blocker as D), leaves Task 1.3 step 5 for the executor to decide ("if it connects eagerly, construct CronTrigger directly"), and its Phase 5 log Verify is a broken pipeline with a `<app log>` placeholder (`grep -cE ... <app log> | grep "ES1!:CME_MINI"`). Paths omit the `src/pocketquant/` prefix throughout.

5. **E (64).** The only candidate to handle `BarBuilderDomainService.add_tick`'s per-tick-delta volume contract (`bar_builder_domain_service.py:71-72`) in the polling quote adapter, and to derive `calendar_id` from `asset_class` in `Symbol.create`; it correctly states there is no PyPI release. But its epoch recovery (`idx.replace(tzinfo=UTC).timestamp()`, Task 5.3 step 4) makes correctness depend on the `TZ=UTC` pin — exactly the trap the advice's don't-list and its own Task 5.4 step 3 forbid — where A's `.timestamp()` on the naive value and D's `df.index.view("int64")` are host-independent. Both Phase 5 Verify URLs are wrong (`ES1%211%3ACME_MINI` decodes to `ES1!1:CME_MINI`), two `grep -c` Verifies expect 2/3 on single-line JSON, Task 5.5 uses `settings.tradingview_max_bars` before Task 5.6 defines it, and Task 4.1's Verify depends on "the test env defaults exported" without saying how.

## Disqualifications

None.

WINNER: A

## Margin

Total margin is 7 points (85 vs 78), which exceeds a quarter of the rubric range. On the deciding criterion — evidence grounding, where the `SyncService` clobber catch lives — A leads B by 3 (17 vs 14), so the lead on the deciding criterion is NOT more than 5 points. Unanimous: **no**. B out-scores A on criterion 3 (handover quality) because A's `uv run ruff check .` gates fail against today's repository.

## Corrections to fold in

The controller materializes A unchanged. Before execution begins, it must know:

1. **A's own gate command (all phases).** Every `uv run ruff check .` in A (Phase 1 Tasks 11 and 13, Phase 2 Task 13, Phase 3 Task 11, Phase 4 Task 8, Phase 6 Task 9, and `plan.md` Success criteria) will fail today with 301 errors and, after DTZ, 21 more under `plans/`. B, C, D and E all scope the command to `uv run ruff check src tests scripts`, which passes now. A's DTZ file list is already scoped to `src/tests/scripts` (34 findings, verified), so only the Verify command is wrong.
2. **tzdata is not installed on Linux (C Task 1.1, D Task 1.7).** `uv.lock` pins PyPI `tzdata` only under `sys_platform == 'win32'`/`'emscripten'` (lines 669, 1283); the venv has no `tzdata` package and `zoneinfo.TZPATH` is the OS database. The audit's "tzdata 2025.3 is locked so zoneinfo works" is therefore wrong as a reason. D's fix (apt `tzdata` in the runtime stage plus a `docker run ... ZoneInfo('America/Chicago')` verify) or C's (`"tzdata>=2025.3"` as an unconditional dependency) should be added to A's Phase 1. Belief, not verified: the `python:3.14-rc-slim` base already ships OS tzdata, so the pin is belt-and-braces rather than a blocker.
3. **Integrity route callers (D Phase 3 inventory, E Task 3.4).** `check_integrity` and `repair_integrity` are also called from `app/routes/integrity.py:33` and `:45`. A's Phase 3 Task 4 threads the calendar only through `sync_jobs.py`; with a required `calendar` parameter the two admin routes would raise `TypeError` at request time. Inject `FromDishka[TradingCalendarFactory]` there as D specifies.
4. **Quote volume must be a per-poll delta (E Task 6.6 step 5).** `BarBuilderDomainService.add_tick` sums `volume` and its docstring requires a per-tick delta (`bar_builder_domain_service.py:68-92`). A's Phase 6 Task 7 emits the bar's total volume on every close change, which double-counts when a 1m bar is polled twice. Emit the difference between successive polls of the same bar, and the full bar volume when the bar rolls.
5. **Same-commit verification (B Task 1.6).** B verifies the hard ordering constraint mechanically with `git show --stat HEAD --name-only` listing exactly the four files; A's Phase 1 Task 6 Verify only runs tests. Add B's check to A's Task 6.
6. **`_can_afford` for contract instruments (B Task 6.5 step 3).** A leaves the check as `fill_price * order.quantity` (points x contracts, not dollars), which passes ES at 4500 x 2 against a 10,000 balance but rejects NQ at ~20,000 x 1. C and D multiply by the multiplier, which rejects every ES entry at the default balance. B's rule — skip the notional bound when `multiplier > 1` and gate on commission only, citing plan 260628-2013's margin model — is the only variant under which G2 runs at the default paper balance without redoing margin accounting. Treat as a design fork to decide before Phase 6, not an improvisation.
7. **`pandas_market_calendars` containment contract (B Task 2.8).** A confines the library by grep in a phase gate; B adds a ninth import-linter `forbidden` contract (`source_modules = core.domain, engine, app`; `forbidden_modules = pandas_market_calendars`), which is enforced on every CI run. Cheap to add to A's Phase 2.
8. **Scraper pin (D Task 5.3 step 1).** A declares `tvdatafeed @ git+https://github.com/rongardF/tvdatafeed.git` unpinned; D asks for a pinned commit or tag because the advice records recurring login breakage. Pin the ref in A's Phase 5 Task 1 once the executor resolves it.
9. **`Symbol.create` should derive `calendar_id` from `asset_class` (E Task 2.9 step 2).** A stores `DEFAULT_CALENDAR_BY_ASSET_CLASS` but `Symbol.create` still defaults `calendar_id` independently; deriving it when the caller omits it is what makes "a new asset class is one enum member plus one schedule record" true at the entity boundary.

## Upstream errors, for the record

- Advice section 9, G1 metric: `GET /api/v1/market-data/bars/ES1!:CME_MINI?interval=1m&limit=1` does not exist; the route is `GET /api/v1/market-data/ohlcv/{symbol}/{interval}` (`app/routes/market_data_ohlcv.py:37`, `app/routes/market_data.py:9`, `core/config.py:44`). All five candidates used the correct route.
- Advice section 2 step 1 and audit P2-1: `GetOHLCVQuery` is a `@dataclass` (`engine/market_data/ohlcv_service.py:13-14`), not a Pydantic model, so "Pydantic field_validator on both DTOs" is wrong for it. A, B, C and D use `__post_init__`; E converts it to `BaseModel` and updates the route construction site.
- Audit P1-3: "tzdata 2025.3 is in uv.lock, so zoneinfo will resolve zones even if the OS database is absent" — the pin carries `sys_platform == 'win32'` / `'emscripten'` markers only (`uv.lock:669,1283`); on Linux `zoneinfo` reads `/usr/share/zoneinfo`. C and D caught this; A, B and E did not.
