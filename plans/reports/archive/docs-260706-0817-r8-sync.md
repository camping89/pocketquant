# R8 Docs Sync Report

**Date:** 2026-07-06 | **Context:** PocketQuant R8 (Live Run Extraction)

## Summary

Synced PocketQuant docs to reflect R8 implementation changes. Verified facts against source code before updating. Generated codebase-summary.md. All edits surgical (no section rewrites).

## Changes Made

### 1. docs/system-architecture.md

**Line 297:** Added missing `trade_repository.py` to repositories list
- Clarifies all 12 repos listed explicitly
- Added comment: "# Live trading: subscription_id FK"

**Line 538-548:** Updated MongoDB section
- Collection count: 12 → 13 (added `trades`)
- Updated join keys description: added `run_id` for backtest records
- Added `trades` collection row to table: uuid7 PK, TradeRepository, "Live trades (avg-cost round-trips)"

**Line 666:** Updated DI providers narrative
- Repo count: 13 → 12 (corrected existing doc error)
- BrokerFactory location: clarified it's in InfrastructureProvider
- ExecutionProvider: added new providers: LiveTradeCollector, LiveMetricsQueryService, StrategyReconcileAppService

**Line 620-622:** Updated live trade pipeline & known issues
- Replaced OKX deferred note with actual R8 implementation
- Added live trade pipeline explanation: TradeClosedEvent → StrategyAppService → EventBus → LiveTradeCollector → TradeRepository
- Added LiveMetricsQueryService flow: on-demand M1 from trades
- Removed stale "defer R8" from known issues list

### 2. docs/code-standards.md

No changes needed (Class Naming table already general enough; LiveTradeCollector/LiveMetricsQueryService not enforcing new suffix conventions).

### 3. docs/codebase-summary.md (NEW)

Generated comprehensive codebase overview:
- Package structure (core/engine/app/web layers)
- 13 MongoDB collections reference table
- Key services (AppServices, Brokers, etc.)
- DI provider chain
- Strategy lifecycle flowchart
- Real-time streaming (inbound WS, outbound SSE, live trade pipeline)
- Backtest flow
- Configuration & startup sequence
- Testing strategy
- Deployment notes
- R8 recent changes summary

---

## Verification Checklist

✅ BrokerFactory location: core/infra/brokers/broker_factory.py (verified)
✅ QuoteAppService + WsSubscriptionAppService: engine/market_data/app_services/ (verified)
✅ TradeRepository: core/infra/persistence/repositories/trade_repository.py (verified)
✅ LiveTradeCollector: engine/live/live_trade_collector.py (verified)
✅ LiveMetricsQueryService: engine/live/live_metrics_query_service.py (verified)
✅ StrategyReconcileAppService: engine/live/strategy_reconcile_app_service.py with bootstrap() method (verified)
✅ Metrics route: GET /api/v1/subscriptions/{sub_id}/metrics in app/routes/strategy.py (verified)
✅ Repo count: 12 repos in _REPO_TYPES (OrderRepository, PositionRepository, BacktestRepository, BacktestOrderRepository, BacktestTradeRepository, BarRepository, SyncStatusRepository, SymbolRepository, JobHistoryRepository, SubscriptionRepository, TrackedSymbolRepository, TradeRepository)

---

## Files Modified

1. `/home/ubuntu-1/W/_me/algotrading/pocketquant/docs/system-architecture.md` — 4 edits
2. `/home/ubuntu-1/W/_me/algotrading/pocketquant/docs/codebase-summary.md` — NEW
3. `repomix-output.xml` — generated (not committed)

---

## Unresolved Questions

None. All R8 facts verified against source code; all references accurate.

---

## Notes

- Doc changes use prose tiếng Việt per CLAUDE.md, code identifiers in English
- No changelog/banner/"previously" language per AS-IS rule
- Component-locator table in system-architecture.md already had LiveTradeCollector/LiveMetricsQueryService (user noted pre-existing)
- Repo count was incorrectly stated as 13 in old doc (line 666), corrected to 12 (matches _REPO_TYPES)
- OKX subscribe_trades remains no-op in broker adapter (deferred to future; live collection via R8 pipeline)
